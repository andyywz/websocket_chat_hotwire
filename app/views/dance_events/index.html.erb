<h1>Dance events</h1>

<ul id="dance_events" role="list" class="divide-y divide-gray-100">
  <%= render @dance_events %>
</ul>

<% if user_signed_in? %>
  <%= link_to "New dance event", new_dance_event_path %>
<% else %>
  You have to be logged in to create dance events
<% end %>
